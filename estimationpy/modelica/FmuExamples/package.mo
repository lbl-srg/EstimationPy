within ;
package FmuExamples "This package contains a simple example that shows hot to use a Modelica model for its identification and state estimation"
annotation (uses(                         Buildings(version="1.6"), Modelica(
        version="3.2.1")));
end FmuExamples;
